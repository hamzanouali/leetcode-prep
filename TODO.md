Create HTML files for the explanation boards to present the following patterns:
NOTE: Check the current HTML files we have, take the templates, and use the same explanation method, style, theme, etc.

# Linked Lists Patterns:
Level 1 (foundations)
  ├── Dummy head node technique
  ├── Reverse a linked list (iterative)
  └── Two pointers (slow/fast)

Level 2 (builds on Level 1)
  ├── Find middle node
  ├── Remove N-th node from end
  ├── Detect cycle
  ├── Merge two sorted lists
  └── Remove duplicates

Level 3 (combines Level 1 + 2)
  ├── Reverse between L and R (LC 92)
  ├── Swap nodes in pairs (K-Group where K=2)
  ├── Palindrome check
  ├── Sort a linked list
  └── Partition / reorder

Level 4 (advanced combos)
  ├── Reverse nodes in K-Group
  ├── Flatten multilevel list
  ├── Deep copy with random pointers
  └── LRU Cache

# Arrays Patterns:
Level 1 (foundations)
  ├── Two pointers — opposite direction (LC 167/125)
  ├── Sliding window — fixed & variable (LC 209/3)
  └── Prefix sum (LC 303/560)

Level 2 (builds on Level 1)
  ├── Binary search (LC 704/35)
  ├── Kadane's algorithm — max subarray (LC 53)
  └── Dutch national flag / sort colors (LC 75)

Level 3 (combines Level 1 + 2)
  ├── 3Sum (LC 15)
  ├── Container with most water (LC 11)
  ├── Merge intervals (LC 56)
  └── Product of array except self (LC 238)

Level 4 (advanced)
  └── Trapping rain water (LC 42)

# Hashmaps Patterns:
Level 1 (foundations)
  ├── Two sum — complement lookup (LC 1)
  └── Frequency counting — anagrams & duplicates (LC 242/217)

Level 2 (builds on Level 1)
  ├── Group anagrams (LC 49)
  └── Longest consecutive sequence (LC 128)

Level 3 (combines techniques)
  ├── Subarray sum equals K — prefix sum + hashmap (LC 560)
  └── Top K frequent elements — freq map + bucket sort (LC 347)
